# VietNam Travel Agent

## How to run locally
1. Create venv, pip install -r requirements.txt
2. Put `travel_data.json` in repo root
3. Set env var OPENWEATHER_KEY if you want weather features.
4. Run: python app.py
